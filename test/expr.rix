func concat(s1: String, s2: String) -> String {
    result := malloc(strlen(s1) + strlen(s2) + 1)

    strcpy(result, s1)
    strcat(result, s2)

    return result
}

thing := concat("sd", "f")
r := "sdf"

if (r == "sdf") {
    print("It did in fact work! (" + r + ")")
} else {
    print("It didn't work! (" + r + ")")
}